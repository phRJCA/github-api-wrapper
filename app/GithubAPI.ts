// TODO: Refactor all functions using the GithubAPI class
/*
  Example:
  const GithubAPI = require("./GithubAPI.ts");

  class orgMethods extends GithubAPI {
  
  };
*/

/*
const https = require("https");
const { USER, TOKEN } = require("../settings.json");
const auth = `${USER}:${TOKEN}`;
const options = {
  auth: auth,
  headers: {
    "user-agent": USER
  }
};

class GithubAPI {
  https = require("https")
  user = USER
  token = TOKEN
  auth = auth
  getOptions = options
}

module.exports = GithubAPI;
*/
